namespace Turnwise.Web;

/// <summary>Display-only product version, shown in the UI to signal this is pre-production software. Unrelated to the file-format schema versions in <see cref="Turnwise.Application.Common.SchemaVersions"/>.</summary>
public static class AppInfo
{
    public const string Version = "0.1.0";
}

namespace Turnwise.Domain.Enums;

/// <summary>Optional tag for a counter, used to pick an icon for its row. A fixed set, same reasoning as <see cref="NamedRollCategory"/>.</summary>
public enum CounterCategory
{
    Ammunition,
    Potions,
    Abilities
}

namespace Turnwise.Domain.Enums;

/// <summary>Optional at-a-glance tag for a combatant, shown in the initiative list. A fixed set rather than free text, so it can be color-coded consistently.</summary>
public enum CombatantCategory
{
    Enemy,
    Ally,
    RP
}
